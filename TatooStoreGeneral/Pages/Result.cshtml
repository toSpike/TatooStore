@page
@model TatooStoreGeneral.Pages.ResultModel
@{
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-sm-10 col-md-8 col-lg-4">
            
            <form method="post">
                <p>@Model.Title</p>
                <button class="btn btn-primary" asp-page-handler="Ok">Ок</button>
            </form>
        </div>
    </div>
</div>